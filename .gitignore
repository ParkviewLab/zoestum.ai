# macOS
.DS_Store

# Secrets — never commit
*recovery_codes*
*.pem
*.key
.env
.env.*
*credentials*
*secret*
*.token

# Build output — assembled by the deploy / preview, never committed (see website.md)
/_site/
